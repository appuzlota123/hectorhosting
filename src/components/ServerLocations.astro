---
const locations = [
  { city: 'New York', country: 'USA', ping: '<10ms', icon: '🇺🇸' },
  { city: 'London', country: 'UK', ping: '<15ms', icon: '🇬🇧' },
  { city: 'Singapore', country: 'SG', ping: '<20ms', icon: '🇸🇬' },
  { city: 'Frankfurt', country: 'DE', ping: '<12ms', icon: '🇩🇪' }
];
---

<div class="py-16">
  <div class="max-w-7xl mx-auto px-6 lg:px-8">
    <div class="text-center mb-12">
      <h2 class="text-3xl font-bold text-white">Global Server Network</h2>
      <p class="mt-4 text-lg text-gray-400">Strategic locations for optimal performance</p>
    </div>

    <div class="grid gap-8 md:grid-cols-4">
      {locations.map(({ city, country, ping, icon }) => (
        <div class="relative group">
          <div class="absolute -inset-0.5 bg-gradient-to-r from-emerald-500/20 to-emerald-600/20 rounded-lg opacity-75 blur group-hover:opacity-100 transition duration-300"></div>
          <div class="relative bg-gray-800/50 backdrop-blur-sm p-6 rounded-lg text-center">
            <div class="text-4xl mb-4">{icon}</div>
            <h3 class="text-xl font-bold text-white mb-2">{city}</h3>
            <p class="text-gray-400">{country}</p>
            <p class="text-emerald-400 mt-2">{ping}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</div>